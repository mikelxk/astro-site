---
import BaseHead from "@components/BaseHead.astro"
import BlogHeader from "@components/BlogHeader.astro"
import BlogPost from "@components/BlogPost.astro"

const { content } = Astro.props
const { title, description, publishDate, name, heroImage, permalink, alt } =
  content
---

<html lang={content.lang || "en"}>
  <head>
    <BaseHead {title} {description} {permalink} />
    <link rel="preload" as="style" href="/prism.css" />
    <link rel="stylesheet" href="/prism.css" />
  </head>

  <body>
    <BlogHeader />
    <div class="wrapper prose prose-img:rounded-lg dark:prose-invert">
      <BlogPost {title} author={name} {heroImage} {publishDate} {alt}>
        <slot />
      </BlogPost>
    </div>
  </body>
</html>
